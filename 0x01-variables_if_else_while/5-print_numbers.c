#include <stdio.h>
/**
 *main - Entry point
 *Description: 'the program's description'
 *Return: Always 0 (Success)
 */
int main(void)
{
int al;
for (al = 0; al <= 9; al++)
{
printf("%d", al);
}
printf("\n");
return (0);
}
